"use client";

import { Navigation } from "@/components/layout";
import React, { useState } from "react";

import { GymListTest, AddGymTest } from "@/components/routes";

const Application = () => {
  return (
    <div>
      < GymListTest />
      {/* < AddGymTest /> */}
    </div>
  );
};

export default Application;
